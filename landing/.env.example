# OmniGuard backend endpoints. Copy to `.env.local` and adjust.
# When unset, the UI runs on local mock data and labels itself an interactive product preview.
VITE_OMNIGUARD_API_BASE=
VITE_OMNIGUARD_DEMO_ROUTE=/demo
